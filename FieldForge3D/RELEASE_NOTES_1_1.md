# FieldForge3D 1.1 – Hidden Bloom

## Highlights

- cleaned up menu bar duplication (`File` and `View` are no longer duplicated)
- promoted **Dancing Eggs** from hidden experiment to visible showcase plugin
- added a new true hidden easter egg plugin:
  - **Event Horizon Bloom**
- polished About dialog and updated app version to **1.1.0**

## Notes

- the hidden plugin is unlocked from the tiny dot in **Help → About**
- `Export all pack...` still skips hidden plugins by default
- STL export remains limited to printable plugins only
